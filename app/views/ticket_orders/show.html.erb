<p style="color: green"><%= notice %></p>

<%= render @ticket_order %>

<div>
  <%= link_to "Edit this ticket order", edit_ticket_order_path(@ticket_order) %> |
  <%= link_to "Back to ticket orders", ticket_orders_path %>

  <%= button_to "Destroy this ticket order", ticket_order_path(@ticket_order), method: :delete %>
</div>
